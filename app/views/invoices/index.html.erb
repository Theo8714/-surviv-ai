<div class="container">
  <h1>Liste des factures</h1>
  <div class="search-bar ">
    <%= form_with url: invoices_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Filtre par numéro de facture"
        %>
      <%= submit_tag "Filtrer", name: "", class: "btn btn-primary" %>
    <% end %>
    </div>
    <div class="row justify-content-start"
         data-controller="progress-search">
      <div class="col-sm-1 my-3 d-flex-column">
        <button class="btn btn-blue my-2"
                style="width: 140px"
                data-action="click->progress-search#searchTraiter"
                >A traiter</button>
        <button class="btn btn-success text-white my-2"
                style="width: 140px"
                data-action="click->progress-search#searchEcheance">
                Avant échéance</button>
        <button class="btn btn-beige my-2"
                style="width: 140px"
                data-action="click->progress-search#searchAmiable">
                Phase amiable</button>
        <button class="btn btn-purple my-2"
                style="width: 140px"
                data-action="click->progress-search#searchJuridique">
                Juridique</button>
      </div>

    <div class="col-sm-11 my-3">
      <div class="cards">
        <% @invoices.each do |invoice|%>
          <div class="card card-beige"
               data-progress-search-target="progress">
            <p>Client : <%= invoice.debtor.company_name %></p>
            <p>Numéro de facture : <strong><%= invoice.number %> </strong></p>
            <p>Montant de l'encours : <%= invoice.amount %>€</p>
            <p>Date d'émission:
              <%= invoice.emission_date %></p>
            <p>Date de l'échéance :
              <%= invoice.payment_date %></p>
              <div>
                <%= link_to "Voir plus", invoice_path(invoice), class: "btn btn-primary" %>
              </div>

          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
